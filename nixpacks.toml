# Railway build configuration
# This ensures SQLite is properly compiled for Linux

[phases.build]
cmds = [
    "npm install",
    "npm rebuild sqlite3 --build-from-source",
    "npm run build"
]

[phases.setup]
nixPkgs = ["nodejs", "npm", "python3", "gcc", "gnumake"]
